import { Wrapper } from "@/components/Wrapper";

const Profile = () => {
  return (
    <Wrapper>
            <h1 className="pageTitle thin">profile</h1>
    </Wrapper>
  );
};

export default Profile;
